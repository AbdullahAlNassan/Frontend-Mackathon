export default function Header() {
  return (
    <header
      className="surface border"
      style={{
        padding: "var(--space-16)",
        fontWeight: 600,
      }}
    >
      <span>AppName</span>
    </header>
  );
}
